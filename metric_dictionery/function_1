def dictionery_of_metrics(items):
    for i in items:
        #if min(items):
            return 'min', min(items)
    #for mean in items:
        #if mean == sum(item)/len(item):
            #return 'mean', mean